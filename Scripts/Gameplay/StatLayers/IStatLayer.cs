namespace Gameplay.StatLayers
{
    /// <summary>
    /// Base interface for all stat layers.
    /// Layers are pure modifiers and contain no references to specific instances.
    /// </summary>
    public interface IStatLayer { }
}